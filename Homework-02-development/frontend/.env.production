# Picked up automatically by `vite build` for production builds (e.g. on
# Vercel). Not a secret — it's just the public backend URL, same one the
# browser calls directly — so this is safe to commit.
VITE_API_BASE_URL=https://tableturn-backend.onrender.com
