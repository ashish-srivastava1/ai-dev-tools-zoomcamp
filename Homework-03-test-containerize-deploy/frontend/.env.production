# Picked up automatically by `vite build` for production builds. Not a
# secret — it's just the public backend URL the browser calls directly —
# so this is safe to commit. Set this once the HW3 backend is deployed.
VITE_API_BASE_URL=
